[package]
name = "lidar_reader"
version = "0.1.0"
edition = "2024"

[dependencies]
if-addrs = "0.13"
thiserror = "2"
tokio = { version = "1", features = ["rt-multi-thread", "net", "time", "macros", "sync"] }
tracing = "0.1"

[dev-dependencies]
tokio-test = "0.4"
